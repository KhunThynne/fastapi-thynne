# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
import datetime

from builtins import int as _int, str as _str
from collections.abc import Iterable, Mapping
from typing import (
    Any,
    Optional,
    cast,
)

LiteralString = str
# -- template models.py.jinja --
import logging
import os
import warnings

from collections import OrderedDict

from . import bases, enums, errors, types
from ._compat import model_rebuild
from .generator import PartialModelField, partial_models_ctx

log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: set[str] = set()

class alembic_version(bases.Basealembic_version):
    """Represents a alembic_version record"""

    version_num: _str

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: bool | None = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Iterable['types.alembic_versionKeys'] | None = None,
        exclude: Iterable['types.alembic_versionKeys'] | None = None,
        required: Iterable['types.alembic_versionKeys'] | None = None,
        optional: Iterable['types.alembic_versionKeys'] | None = None,
        relations: Mapping['types.alembic_versionRelationalFieldKeys', str] | None = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: dict[types.alembic_versionKeys, PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _alembic_version_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _alembic_version_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _alembic_version_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _alembic_version_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "alembic_version" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid alembic_version / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'alembic_version',
            }
        )
        _created_partial_types.add(name)


class license(bases.Baselicense):
    """Represents a license record"""

    key: _str
    product_id: _str
    owner_id: _str | None = None
    activated_at: datetime.datetime | None = None
    expired_at: datetime.datetime | None = None
    id: _str
    users: Optional['models.users'] = None
    products: Optional['models.products'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: bool | None = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Iterable['types.licenseKeys'] | None = None,
        exclude: Iterable['types.licenseKeys'] | None = None,
        required: Iterable['types.licenseKeys'] | None = None,
        optional: Iterable['types.licenseKeys'] | None = None,
        relations: Mapping['types.licenseRelationalFieldKeys', str] | None = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: dict[types.licenseKeys, PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _license_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _license_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _license_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _license_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _license_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _license_relational_fields:
                        raise errors.UnknownRelationalFieldError('license', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid license / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'license',
            }
        )
        _created_partial_types.add(name)


class products(bases.Baseproducts):
    """Represents a products record"""

    type: 'enums.productstypeenum'
    duration_days: _int
    id: _str
    name: _str
    licenses: list['models.license'] | None = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: bool | None = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Iterable['types.productsKeys'] | None = None,
        exclude: Iterable['types.productsKeys'] | None = None,
        required: Iterable['types.productsKeys'] | None = None,
        optional: Iterable['types.productsKeys'] | None = None,
        relations: Mapping['types.productsRelationalFieldKeys', str] | None = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: dict[types.productsKeys, PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _products_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _products_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _products_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _products_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _products_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _products_relational_fields:
                        raise errors.UnknownRelationalFieldError('products', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid products / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'products',
            }
        )
        _created_partial_types.add(name)


class users(bases.Baseusers):
    """Represents a users record"""

    username: _str
    email: _str
    id: _str
    licenses: list['models.license'] | None = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: bool | None = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Iterable['types.usersKeys'] | None = None,
        exclude: Iterable['types.usersKeys'] | None = None,
        required: Iterable['types.usersKeys'] | None = None,
        optional: Iterable['types.usersKeys'] | None = None,
        relations: Mapping['types.usersRelationalFieldKeys', str] | None = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: dict[types.usersKeys, PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _users_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _users_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _users_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _users_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _users_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _users_relational_fields:
                        raise errors.UnknownRelationalFieldError('users', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid users / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'users',
            }
        )
        _created_partial_types.add(name)



_alembic_version_relational_fields: set[str] = set()  # pyright: ignore[reportUnusedVariable]
_alembic_version_fields: dict['types.alembic_versionKeys', PartialModelField] = OrderedDict(
    [
        ('version_num', {
            'name': 'version_num',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_license_relational_fields: set[str] = {
        'users',
        'products',
    }
_license_fields: dict['types.licenseKeys', PartialModelField] = OrderedDict(
    [
        ('key', {
            'name': 'key',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('product_id', {
            'name': 'product_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('owner_id', {
            'name': 'owner_id',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('activated_at', {
            'name': 'activated_at',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('expired_at', {
            'name': 'expired_at',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('users', {
            'name': 'users',
            'is_list': False,
            'optional': True,
            'type': 'models.users',
            'is_relational': True,
            'documentation': None,
        }),
        ('products', {
            'name': 'products',
            'is_list': False,
            'optional': True,
            'type': 'models.products',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_products_relational_fields: set[str] = {
        'licenses',
    }
_products_fields: dict['types.productsKeys', PartialModelField] = OrderedDict(
    [
        ('type', {
            'name': 'type',
            'is_list': False,
            'optional': False,
            'type': 'enums.productstypeenum',
            'is_relational': False,
            'documentation': None,
        }),
        ('duration_days', {
            'name': 'duration_days',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('licenses', {
            'name': 'licenses',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.license\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_users_relational_fields: set[str] = {
        'licenses',
    }
_users_fields: dict['types.usersKeys', PartialModelField] = OrderedDict(
    [
        ('username', {
            'name': 'username',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('email', {
            'name': 'email',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('licenses', {
            'name': 'licenses',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.license\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models

# required to support relationships between models
model_rebuild(alembic_version)
model_rebuild(license)
model_rebuild(products)
model_rebuild(users)
