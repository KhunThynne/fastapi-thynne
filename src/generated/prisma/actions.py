# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

from collections.abc import Mapping

# global imports for type checking
from typing import (
    TYPE_CHECKING,
    Any,
    Generic,
    TypeVar,
    cast,
    overload,
)

LiteralString = str
# -- template actions.py.jinja --

from . import errors, types
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .bases import _PrismaModel
    from .client import Prisma


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class alembic_versionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> list[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.alembic_version]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await alembic_version.prisma().query_raw(
            'SELECT * FROM alembic_version WHERE version_num = $1',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> _PrismaModelT | None:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.alembic_version
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await alembic_version.prisma().query_first(
            'SELECT * FROM alembic_version WHERE version_num = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.alembic_versionCreateInput,
        include: types.alembic_versionInclude | None = None
    ) -> _PrismaModelT:
        """Create a new alembic_version record.

        Parameters
        ----------
        data
            alembic_version record data
        include
            Specifies which relations should be loaded on the returned alembic_version model

        Returns
        -------
        prisma.models.alembic_version
            The created alembic_version record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a alembic_version record from just the required fields
        alembic_version = await alembic_version.prisma().create(
            data={
                # data to create a alembic_version record
                'version_num': 'ggciceaie',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: list[types.alembic_versionCreateWithoutRelationsInput],
        *,
        skip_duplicates: bool | None = None,
    ) -> int:
        """Create multiple alembic_version records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of alembic_version record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await alembic_version.prisma().create_many(
            data=[
                {
                    # data to create a alembic_version record
                    'version_num': 'bbehjachib',
                },
                {
                    # data to create a alembic_version record
                    'version_num': 'cadfabfehe',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.alembic_versionWhereUniqueInput,
        include: types.alembic_versionInclude | None = None
    ) -> _PrismaModelT | None:
        """Delete a single alembic_version record.

        Parameters
        ----------
        where
            alembic_version filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned alembic_version model

        Returns
        -------
        prisma.models.alembic_version
            The deleted alembic_version record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        alembic_version = await alembic_version.prisma().delete(
            where={
                'version_num': 'dgiiaaijj',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.alembic_versionWhereUniqueInput,
        include: types.alembic_versionInclude | None = None
    ) -> _PrismaModelT | None:
        """Find a unique alembic_version record.

        Parameters
        ----------
        where
            alembic_version filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned alembic_version model

        Returns
        -------
        prisma.models.alembic_version
            The found alembic_version record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        alembic_version = await alembic_version.prisma().find_unique(
            where={
                'version_num': 'bfaiacjjfc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.alembic_versionWhereUniqueInput,
        include: types.alembic_versionInclude | None = None
    ) -> _PrismaModelT:
        """Find a unique alembic_version record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            alembic_version filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned alembic_version model

        Returns
        -------
        prisma.models.alembic_version
            The found alembic_version record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        alembic_version = await alembic_version.prisma().find_unique_or_raise(
            where={
                'version_num': 'eigcfgbif',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: int | None = None,
        skip: int | None = None,
        where: types.alembic_versionWhereInput | None = None,
        cursor: types.alembic_versionWhereUniqueInput | None = None,
        include: types.alembic_versionInclude | None = None,
        order: types.alembic_versionOrderByInput | list[types.alembic_versionOrderByInput] | None = None,
        distinct: list[types.alembic_versionScalarFieldKeys] | None = None,
    ) -> list[_PrismaModelT]:
        """Find multiple alembic_version records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of alembic_version records returned
        skip
            Ignore the first N results
        where
            alembic_version filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned alembic_version model
        order
            Order the returned alembic_version records by any field
        distinct
            Filter alembic_version records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.alembic_version]
            The list of all alembic_version records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 alembic_version records
        alembic_versions = await alembic_version.prisma().find_many(take=10)

        # find the first 5 alembic_version records ordered by the version_num field
        alembic_versions = await alembic_version.prisma().find_many(
            take=5,
            order={
                'version_num': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: int | None = None,
        where: types.alembic_versionWhereInput | None = None,
        cursor: types.alembic_versionWhereUniqueInput | None = None,
        include: types.alembic_versionInclude | None = None,
        order: types.alembic_versionOrderByInput | list[types.alembic_versionOrderByInput] | None = None,
        distinct: list[types.alembic_versionScalarFieldKeys] | None = None,
    ) -> _PrismaModelT | None:
        """Find a single alembic_version record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            alembic_version filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned alembic_version model
        order
            Order the returned alembic_version records by any field
        distinct
            Filter alembic_version records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.alembic_version
            The first alembic_version record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second alembic_version record ordered by the version_num field
        alembic_version = await alembic_version.prisma().find_first(
            skip=1,
            order={
                'version_num': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: int | None = None,
        where: types.alembic_versionWhereInput | None = None,
        cursor: types.alembic_versionWhereUniqueInput | None = None,
        include: types.alembic_versionInclude | None = None,
        order: types.alembic_versionOrderByInput | list[types.alembic_versionOrderByInput] | None = None,
        distinct: list[types.alembic_versionScalarFieldKeys] | None = None,
    ) -> _PrismaModelT:
        """Find a single alembic_version record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            alembic_version filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned alembic_version model
        order
            Order the returned alembic_version records by any field
        distinct
            Filter alembic_version records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.alembic_version
            The first alembic_version record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second alembic_version record ordered by the version_num field
        alembic_version = await alembic_version.prisma().find_first_or_raise(
            skip=1,
            order={
                'version_num': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.alembic_versionUpdateInput,
        where: types.alembic_versionWhereUniqueInput,
        include: types.alembic_versionInclude | None = None
    ) -> _PrismaModelT | None:
        """Update a single alembic_version record.

        Parameters
        ----------
        data
            alembic_version record data specifying what to update
        where
            alembic_version filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned alembic_version model

        Returns
        -------
        prisma.models.alembic_version
            The updated alembic_version record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        alembic_version = await alembic_version.prisma().update(
            where={
                'version_num': 'bagcfbhiig',
            },
            data={
                # data to update the alembic_version record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.alembic_versionWhereUniqueInput,
        data: types.alembic_versionUpsertInput,
        include: types.alembic_versionInclude | None = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            alembic_version filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned alembic_version model

        Returns
        -------
        prisma.models.alembic_version
            The created or updated alembic_version record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        alembic_version = await alembic_version.prisma().upsert(
            where={
                'version_num': 'cghideieh',
            },
            data={
                'create': {
                    'version_num': 'cghideieh',
                },
                'update': {
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.alembic_versionUpdateManyMutationInput,
        where: types.alembic_versionWhereInput,
    ) -> int:
        """Update multiple alembic_version records

        Parameters
        ----------
        data
            alembic_version data to update the selected alembic_version records to
        where
            Filter to select the alembic_version records to update

        Returns
        -------
        int
            The total number of alembic_version records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all alembic_version records
        total = await alembic_version.prisma().update_many(
            data={
                'version_num': 'biabhbdai'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: int | None = None,
        skip: int | None = None,
        where: types.alembic_versionWhereInput | None = None,
        cursor: types.alembic_versionWhereUniqueInput | None = None,
    ) -> int:
        """Count the number of alembic_version records present in the database

        Parameters
        ----------
        select
            Select the alembic_version fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            alembic_version filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.alembic_versionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await alembic_version.prisma().count()

        # results: prisma.types.alembic_versionCountAggregateOutput
        results = await alembic_version.prisma().count(
            select={
                '_all': True,
                'version_num': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.alembic_versionCountAggregateInput,
        take: int | None = None,
        skip: int | None = None,
        where: types.alembic_versionWhereInput | None = None,
        cursor: types.alembic_versionWhereUniqueInput | None = None,
    ) -> types.alembic_versionCountAggregateOutput:
        ...

    async def count(
        self,
        select: types.alembic_versionCountAggregateInput | None = None,
        take: int | None = None,
        skip: int | None = None,
        where: types.alembic_versionWhereInput | None = None,
        cursor: types.alembic_versionWhereUniqueInput | None = None,
    ) -> int | types.alembic_versionCountAggregateOutput:
        """Count the number of alembic_version records present in the database

        Parameters
        ----------
        select
            Select the alembic_version fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            alembic_version filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.alembic_versionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await alembic_version.prisma().count()

        # results: prisma.types.alembic_versionCountAggregateOutput
        results = await alembic_version.prisma().count(
            select={
                '_all': True,
                'version_num': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.alembic_versionCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: types.alembic_versionWhereInput | None = None
    ) -> int:
        """Delete multiple alembic_version records.

        Parameters
        ----------
        where
            Optional alembic_version filter to find the records to be deleted

        Returns
        -------
        int
            The total number of alembic_version records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all alembic_version records
        total = await alembic_version.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: list[types.alembic_versionScalarFieldKeys],
        *,
        where: types.alembic_versionWhereInput | None = None,
        take: int | None = None,
        skip: int | None = None,
        avg: types.alembic_versionAvgAggregateInput | None = None,
        sum: types.alembic_versionSumAggregateInput | None = None,
        min: types.alembic_versionMinAggregateInput | None = None,
        max: types.alembic_versionMaxAggregateInput | None = None,
        having: types.alembic_versionScalarWhereWithAggregatesInput | None = None,
        count: bool | types.alembic_versionCountAggregateInput | None = None,
        order: Mapping[types.alembic_versionScalarFieldKeys, types.SortOrder] | list[Mapping[types.alembic_versionScalarFieldKeys, types.SortOrder]] | None = None,
    ) -> list[types.alembic_versionGroupByOutput]:
        """Group alembic_version records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar alembic_version fields to group records by
        where
            alembic_version filter to select records
        take
            Limit the maximum number of alembic_version records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.alembic_versionGroupByOutput]
            A list of dictionaries representing the alembic_version record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group alembic_version records by version_num values
        # and count how many records are in each group
        results = await alembic_version.prisma().group_by(
            ['version_num'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: list[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class licenseActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> list[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.license]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await license.prisma().query_raw(
            'SELECT * FROM license WHERE key = $1',
            'idghgaicb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> _PrismaModelT | None:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.license
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await license.prisma().query_first(
            'SELECT * FROM license WHERE product_id = $1',
            'fjfddhigg',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.licenseCreateInput,
        include: types.licenseInclude | None = None
    ) -> _PrismaModelT:
        """Create a new license record.

        Parameters
        ----------
        data
            license record data
        include
            Specifies which relations should be loaded on the returned license model

        Returns
        -------
        prisma.models.license
            The created license record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a license record from just the required fields
        license = await license.prisma().create(
            data={
                # data to create a license record
                'key': 'hjaecfifb',
                'product_id': 'cbbbjbfcii',
                'id': 'bbejhfidcb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: list[types.licenseCreateWithoutRelationsInput],
        *,
        skip_duplicates: bool | None = None,
    ) -> int:
        """Create multiple license records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of license record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await license.prisma().create_many(
            data=[
                {
                    # data to create a license record
                    'key': 'bgeecijdgg',
                    'product_id': 'bdiicjafbj',
                    'id': 'bgehebiafc',
                },
                {
                    # data to create a license record
                    'key': 'bghffegacj',
                    'product_id': 'bhghchehcc',
                    'id': 'dcgchcbbf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.licenseWhereUniqueInput,
        include: types.licenseInclude | None = None
    ) -> _PrismaModelT | None:
        """Delete a single license record.

        Parameters
        ----------
        where
            license filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned license model

        Returns
        -------
        prisma.models.license
            The deleted license record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        license = await license.prisma().delete(
            where={
                # license where unique filter

            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.licenseWhereUniqueInput,
        include: types.licenseInclude | None = None
    ) -> _PrismaModelT | None:
        """Find a unique license record.

        Parameters
        ----------
        where
            license filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned license model

        Returns
        -------
        prisma.models.license
            The found license record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        license = await license.prisma().find_unique(
            where={
                # license where unique filter

            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.licenseWhereUniqueInput,
        include: types.licenseInclude | None = None
    ) -> _PrismaModelT:
        """Find a unique license record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            license filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned license model

        Returns
        -------
        prisma.models.license
            The found license record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        license = await license.prisma().find_unique_or_raise(
            where={
                # license where unique filter

            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: int | None = None,
        skip: int | None = None,
        where: types.licenseWhereInput | None = None,
        cursor: types.licenseWhereUniqueInput | None = None,
        include: types.licenseInclude | None = None,
        order: types.licenseOrderByInput | list[types.licenseOrderByInput] | None = None,
        distinct: list[types.licenseScalarFieldKeys] | None = None,
    ) -> list[_PrismaModelT]:
        """Find multiple license records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of license records returned
        skip
            Ignore the first N results
        where
            license filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned license model
        order
            Order the returned license records by any field
        distinct
            Filter license records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.license]
            The list of all license records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 license records
        licenses = await license.prisma().find_many(take=10)

        # find the first 5 license records ordered by the owner_id field
        licenses = await license.prisma().find_many(
            take=5,
            order={
                'owner_id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: int | None = None,
        where: types.licenseWhereInput | None = None,
        cursor: types.licenseWhereUniqueInput | None = None,
        include: types.licenseInclude | None = None,
        order: types.licenseOrderByInput | list[types.licenseOrderByInput] | None = None,
        distinct: list[types.licenseScalarFieldKeys] | None = None,
    ) -> _PrismaModelT | None:
        """Find a single license record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            license filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned license model
        order
            Order the returned license records by any field
        distinct
            Filter license records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.license
            The first license record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second license record ordered by the activated_at field
        license = await license.prisma().find_first(
            skip=1,
            order={
                'activated_at': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: int | None = None,
        where: types.licenseWhereInput | None = None,
        cursor: types.licenseWhereUniqueInput | None = None,
        include: types.licenseInclude | None = None,
        order: types.licenseOrderByInput | list[types.licenseOrderByInput] | None = None,
        distinct: list[types.licenseScalarFieldKeys] | None = None,
    ) -> _PrismaModelT:
        """Find a single license record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            license filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned license model
        order
            Order the returned license records by any field
        distinct
            Filter license records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.license
            The first license record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second license record ordered by the expired_at field
        license = await license.prisma().find_first_or_raise(
            skip=1,
            order={
                'expired_at': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.licenseUpdateInput,
        where: types.licenseWhereUniqueInput,
        include: types.licenseInclude | None = None
    ) -> _PrismaModelT | None:
        """Update a single license record.

        Parameters
        ----------
        data
            license record data specifying what to update
        where
            license filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned license model

        Returns
        -------
        prisma.models.license
            The updated license record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        license = await license.prisma().update(
            where={
                # license where unique filter

            },
            data={
                # data to update the license record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.licenseWhereUniqueInput,
        data: types.licenseUpsertInput,
        include: types.licenseInclude | None = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            license filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned license model

        Returns
        -------
        prisma.models.license
            The created or updated license record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        license = await license.prisma().upsert(
            where={
                # license where unique filter
            },
            data={
                'create': {
                    # license data to be set if the record does not exist
                },
                'update': {
                    # license data to be set if the record does exist
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.licenseUpdateManyMutationInput,
        where: types.licenseWhereInput,
    ) -> int:
        """Update multiple license records

        Parameters
        ----------
        data
            license data to update the selected license records to
        where
            Filter to select the license records to update

        Returns
        -------
        int
            The total number of license records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all license records
        total = await license.prisma().update_many(
            data={
                'id': 'bdedcabahc'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: int | None = None,
        skip: int | None = None,
        where: types.licenseWhereInput | None = None,
        cursor: types.licenseWhereUniqueInput | None = None,
    ) -> int:
        """Count the number of license records present in the database

        Parameters
        ----------
        select
            Select the license fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            license filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.licenseCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await license.prisma().count()

        # results: prisma.types.licenseCountAggregateOutput
        results = await license.prisma().count(
            select={
                '_all': True,
                'key': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.licenseCountAggregateInput,
        take: int | None = None,
        skip: int | None = None,
        where: types.licenseWhereInput | None = None,
        cursor: types.licenseWhereUniqueInput | None = None,
    ) -> types.licenseCountAggregateOutput:
        ...

    async def count(
        self,
        select: types.licenseCountAggregateInput | None = None,
        take: int | None = None,
        skip: int | None = None,
        where: types.licenseWhereInput | None = None,
        cursor: types.licenseWhereUniqueInput | None = None,
    ) -> int | types.licenseCountAggregateOutput:
        """Count the number of license records present in the database

        Parameters
        ----------
        select
            Select the license fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            license filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.licenseCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await license.prisma().count()

        # results: prisma.types.licenseCountAggregateOutput
        results = await license.prisma().count(
            select={
                '_all': True,
                'product_id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.licenseCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: types.licenseWhereInput | None = None
    ) -> int:
        """Delete multiple license records.

        Parameters
        ----------
        where
            Optional license filter to find the records to be deleted

        Returns
        -------
        int
            The total number of license records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all license records
        total = await license.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: list[types.licenseScalarFieldKeys],
        *,
        where: types.licenseWhereInput | None = None,
        take: int | None = None,
        skip: int | None = None,
        avg: types.licenseAvgAggregateInput | None = None,
        sum: types.licenseSumAggregateInput | None = None,
        min: types.licenseMinAggregateInput | None = None,
        max: types.licenseMaxAggregateInput | None = None,
        having: types.licenseScalarWhereWithAggregatesInput | None = None,
        count: bool | types.licenseCountAggregateInput | None = None,
        order: Mapping[types.licenseScalarFieldKeys, types.SortOrder] | list[Mapping[types.licenseScalarFieldKeys, types.SortOrder]] | None = None,
    ) -> list[types.licenseGroupByOutput]:
        """Group license records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar license fields to group records by
        where
            license filter to select records
        take
            Limit the maximum number of license records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.licenseGroupByOutput]
            A list of dictionaries representing the license record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group license records by owner_id values
        # and count how many records are in each group
        results = await license.prisma().group_by(
            ['owner_id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: list[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class productsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> list[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.products]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await products.prisma().query_raw(
            'SELECT * FROM products WHERE type = $1',
            enums.productstypeenum.PRO,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> _PrismaModelT | None:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.products
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await products.prisma().query_first(
            'SELECT * FROM products WHERE duration_days = $1',
            675780521,
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.productsCreateInput,
        include: types.productsInclude | None = None
    ) -> _PrismaModelT:
        """Create a new products record.

        Parameters
        ----------
        data
            products record data
        include
            Specifies which relations should be loaded on the returned products model

        Returns
        -------
        prisma.models.products
            The created products record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a products record from just the required fields
        products = await products.prisma().create(
            data={
                # data to create a products record
                'type': enums.productstypeenum.PRO,
                'duration_days': 744964398,
                'id': 'bjgjgibgbf',
                'name': 'bbbgbhfjge',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: list[types.productsCreateWithoutRelationsInput],
        *,
        skip_duplicates: bool | None = None,
    ) -> int:
        """Create multiple products records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of products record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await products.prisma().create_many(
            data=[
                {
                    # data to create a products record
                    'type': enums.productstypeenum.PRO,
                    'duration_days': 861472101,
                    'id': 'bdadaadhag',
                    'name': 'bgiggdidbf',
                },
                {
                    # data to create a products record
                    'type': enums.productstypeenum.PRO,
                    'duration_days': 2000430152,
                    'id': 'bigibebcib',
                    'name': 'bigaiehgcc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.productsWhereUniqueInput,
        include: types.productsInclude | None = None
    ) -> _PrismaModelT | None:
        """Delete a single products record.

        Parameters
        ----------
        where
            products filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned products model

        Returns
        -------
        prisma.models.products
            The deleted products record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        products = await products.prisma().delete(
            where={
                'id': 'beeifcbebf',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.productsWhereUniqueInput,
        include: types.productsInclude | None = None
    ) -> _PrismaModelT | None:
        """Find a unique products record.

        Parameters
        ----------
        where
            products filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned products model

        Returns
        -------
        prisma.models.products
            The found products record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        products = await products.prisma().find_unique(
            where={
                'id': 'bgcigfahea',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.productsWhereUniqueInput,
        include: types.productsInclude | None = None
    ) -> _PrismaModelT:
        """Find a unique products record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            products filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned products model

        Returns
        -------
        prisma.models.products
            The found products record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        products = await products.prisma().find_unique_or_raise(
            where={
                'id': 'bcejgaggif',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: int | None = None,
        skip: int | None = None,
        where: types.productsWhereInput | None = None,
        cursor: types.productsWhereUniqueInput | None = None,
        include: types.productsInclude | None = None,
        order: types.productsOrderByInput | list[types.productsOrderByInput] | None = None,
        distinct: list[types.productsScalarFieldKeys] | None = None,
    ) -> list[_PrismaModelT]:
        """Find multiple products records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of products records returned
        skip
            Ignore the first N results
        where
            products filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned products model
        order
            Order the returned products records by any field
        distinct
            Filter products records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.products]
            The list of all products records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 products records
        products = await products.prisma().find_many(take=10)

        # find the first 5 products records ordered by the id field
        products = await products.prisma().find_many(
            take=5,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: int | None = None,
        where: types.productsWhereInput | None = None,
        cursor: types.productsWhereUniqueInput | None = None,
        include: types.productsInclude | None = None,
        order: types.productsOrderByInput | list[types.productsOrderByInput] | None = None,
        distinct: list[types.productsScalarFieldKeys] | None = None,
    ) -> _PrismaModelT | None:
        """Find a single products record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            products filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned products model
        order
            Order the returned products records by any field
        distinct
            Filter products records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.products
            The first products record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second products record ordered by the name field
        products = await products.prisma().find_first(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: int | None = None,
        where: types.productsWhereInput | None = None,
        cursor: types.productsWhereUniqueInput | None = None,
        include: types.productsInclude | None = None,
        order: types.productsOrderByInput | list[types.productsOrderByInput] | None = None,
        distinct: list[types.productsScalarFieldKeys] | None = None,
    ) -> _PrismaModelT:
        """Find a single products record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            products filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned products model
        order
            Order the returned products records by any field
        distinct
            Filter products records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.products
            The first products record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second products record ordered by the type field
        products = await products.prisma().find_first_or_raise(
            skip=1,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.productsUpdateInput,
        where: types.productsWhereUniqueInput,
        include: types.productsInclude | None = None
    ) -> _PrismaModelT | None:
        """Update a single products record.

        Parameters
        ----------
        data
            products record data specifying what to update
        where
            products filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned products model

        Returns
        -------
        prisma.models.products
            The updated products record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        products = await products.prisma().update(
            where={
                'id': 'idfjadbcc',
            },
            data={
                # data to update the products record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.productsWhereUniqueInput,
        data: types.productsUpsertInput,
        include: types.productsInclude | None = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            products filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned products model

        Returns
        -------
        prisma.models.products
            The created or updated products record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        products = await products.prisma().upsert(
            where={
                'id': 'hgdhbjhhj',
            },
            data={
                'create': {
                    'id': 'hgdhbjhhj',
                    'type': enums.productstypeenum.PRO,
                    'duration_days': 2000430152,
                    'name': 'bigaiehgcc',
                },
                'update': {
                    'type': enums.productstypeenum.PRO,
                    'duration_days': 2000430152,
                    'name': 'bigaiehgcc',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.productsUpdateManyMutationInput,
        where: types.productsWhereInput,
    ) -> int:
        """Update multiple products records

        Parameters
        ----------
        data
            products data to update the selected products records to
        where
            Filter to select the products records to update

        Returns
        -------
        int
            The total number of products records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all products records
        total = await products.prisma().update_many(
            data={
                'duration_days': 429995104
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: int | None = None,
        skip: int | None = None,
        where: types.productsWhereInput | None = None,
        cursor: types.productsWhereUniqueInput | None = None,
    ) -> int:
        """Count the number of products records present in the database

        Parameters
        ----------
        select
            Select the products fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            products filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.productsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await products.prisma().count()

        # results: prisma.types.productsCountAggregateOutput
        results = await products.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.productsCountAggregateInput,
        take: int | None = None,
        skip: int | None = None,
        where: types.productsWhereInput | None = None,
        cursor: types.productsWhereUniqueInput | None = None,
    ) -> types.productsCountAggregateOutput:
        ...

    async def count(
        self,
        select: types.productsCountAggregateInput | None = None,
        take: int | None = None,
        skip: int | None = None,
        where: types.productsWhereInput | None = None,
        cursor: types.productsWhereUniqueInput | None = None,
    ) -> int | types.productsCountAggregateOutput:
        """Count the number of products records present in the database

        Parameters
        ----------
        select
            Select the products fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            products filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.productsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await products.prisma().count()

        # results: prisma.types.productsCountAggregateOutput
        results = await products.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.productsCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: types.productsWhereInput | None = None
    ) -> int:
        """Delete multiple products records.

        Parameters
        ----------
        where
            Optional products filter to find the records to be deleted

        Returns
        -------
        int
            The total number of products records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all products records
        total = await products.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: list[types.productsScalarFieldKeys],
        *,
        where: types.productsWhereInput | None = None,
        take: int | None = None,
        skip: int | None = None,
        avg: types.productsAvgAggregateInput | None = None,
        sum: types.productsSumAggregateInput | None = None,
        min: types.productsMinAggregateInput | None = None,
        max: types.productsMaxAggregateInput | None = None,
        having: types.productsScalarWhereWithAggregatesInput | None = None,
        count: bool | types.productsCountAggregateInput | None = None,
        order: Mapping[types.productsScalarFieldKeys, types.SortOrder] | list[Mapping[types.productsScalarFieldKeys, types.SortOrder]] | None = None,
    ) -> list[types.productsGroupByOutput]:
        """Group products records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar products fields to group records by
        where
            products filter to select records
        take
            Limit the maximum number of products records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.productsGroupByOutput]
            A list of dictionaries representing the products record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group products records by type values
        # and count how many records are in each group
        results = await products.prisma().group_by(
            ['type'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: list[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class usersActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> list[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.users]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await users.prisma().query_raw(
            'SELECT * FROM users WHERE username = $1',
            'bhhfibbigf',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> _PrismaModelT | None:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.users
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await users.prisma().query_first(
            'SELECT * FROM users WHERE email = $1',
            'ijdbeffgg',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.usersCreateInput,
        include: types.usersInclude | None = None
    ) -> _PrismaModelT:
        """Create a new users record.

        Parameters
        ----------
        data
            users record data
        include
            Specifies which relations should be loaded on the returned users model

        Returns
        -------
        prisma.models.users
            The created users record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a users record from just the required fields
        users = await users.prisma().create(
            data={
                # data to create a users record
                'username': 'jjfeafhfj',
                'email': 'cbachdgfce',
                'id': 'chbfcacbd',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: list[types.usersCreateWithoutRelationsInput],
        *,
        skip_duplicates: bool | None = None,
    ) -> int:
        """Create multiple users records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of users record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await users.prisma().create_many(
            data=[
                {
                    # data to create a users record
                    'username': 'efggddide',
                    'email': 'caficfigfb',
                    'id': 'bfidgijfjc',
                },
                {
                    # data to create a users record
                    'username': 'ihieecagf',
                    'email': 'bghfciaafe',
                    'id': 'bgchfhgceh',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.usersWhereUniqueInput,
        include: types.usersInclude | None = None
    ) -> _PrismaModelT | None:
        """Delete a single users record.

        Parameters
        ----------
        where
            users filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned users model

        Returns
        -------
        prisma.models.users
            The deleted users record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        users = await users.prisma().delete(
            where={
                'username': 'cafeiaccbc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.usersWhereUniqueInput,
        include: types.usersInclude | None = None
    ) -> _PrismaModelT | None:
        """Find a unique users record.

        Parameters
        ----------
        where
            users filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned users model

        Returns
        -------
        prisma.models.users
            The found users record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        users = await users.prisma().find_unique(
            where={
                'username': 'gaddfhfh',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.usersWhereUniqueInput,
        include: types.usersInclude | None = None
    ) -> _PrismaModelT:
        """Find a unique users record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            users filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned users model

        Returns
        -------
        prisma.models.users
            The found users record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        users = await users.prisma().find_unique_or_raise(
            where={
                'username': 'gieegcbeg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: int | None = None,
        skip: int | None = None,
        where: types.usersWhereInput | None = None,
        cursor: types.usersWhereUniqueInput | None = None,
        include: types.usersInclude | None = None,
        order: types.usersOrderByInput | list[types.usersOrderByInput] | None = None,
        distinct: list[types.usersScalarFieldKeys] | None = None,
    ) -> list[_PrismaModelT]:
        """Find multiple users records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of users records returned
        skip
            Ignore the first N results
        where
            users filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned users model
        order
            Order the returned users records by any field
        distinct
            Filter users records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.users]
            The list of all users records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 users records
        users = await users.prisma().find_many(take=10)

        # find the first 5 users records ordered by the id field
        users = await users.prisma().find_many(
            take=5,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: int | None = None,
        where: types.usersWhereInput | None = None,
        cursor: types.usersWhereUniqueInput | None = None,
        include: types.usersInclude | None = None,
        order: types.usersOrderByInput | list[types.usersOrderByInput] | None = None,
        distinct: list[types.usersScalarFieldKeys] | None = None,
    ) -> _PrismaModelT | None:
        """Find a single users record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            users filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned users model
        order
            Order the returned users records by any field
        distinct
            Filter users records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.users
            The first users record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second users record ordered by the username field
        users = await users.prisma().find_first(
            skip=1,
            order={
                'username': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: int | None = None,
        where: types.usersWhereInput | None = None,
        cursor: types.usersWhereUniqueInput | None = None,
        include: types.usersInclude | None = None,
        order: types.usersOrderByInput | list[types.usersOrderByInput] | None = None,
        distinct: list[types.usersScalarFieldKeys] | None = None,
    ) -> _PrismaModelT:
        """Find a single users record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            users filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned users model
        order
            Order the returned users records by any field
        distinct
            Filter users records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.users
            The first users record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second users record ordered by the email field
        users = await users.prisma().find_first_or_raise(
            skip=1,
            order={
                'email': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.usersUpdateInput,
        where: types.usersWhereUniqueInput,
        include: types.usersInclude | None = None
    ) -> _PrismaModelT | None:
        """Update a single users record.

        Parameters
        ----------
        data
            users record data specifying what to update
        where
            users filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned users model

        Returns
        -------
        prisma.models.users
            The updated users record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await users.prisma().update(
            where={
                'username': 'bgcffadich',
            },
            data={
                # data to update the users record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.usersWhereUniqueInput,
        data: types.usersUpsertInput,
        include: types.usersInclude | None = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            users filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned users model

        Returns
        -------
        prisma.models.users
            The created or updated users record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        users = await users.prisma().upsert(
            where={
                'username': 'fcbichhci',
            },
            data={
                'create': {
                    'username': 'fcbichhci',
                    'email': 'bghfciaafe',
                    'id': 'bgchfhgceh',
                },
                'update': {
                    'email': 'bghfciaafe',
                    'id': 'bgchfhgceh',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.usersUpdateManyMutationInput,
        where: types.usersWhereInput,
    ) -> int:
        """Update multiple users records

        Parameters
        ----------
        data
            users data to update the selected users records to
        where
            Filter to select the users records to update

        Returns
        -------
        int
            The total number of users records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all users records
        total = await users.prisma().update_many(
            data={
                'id': 'bcggadccgf'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: int | None = None,
        skip: int | None = None,
        where: types.usersWhereInput | None = None,
        cursor: types.usersWhereUniqueInput | None = None,
    ) -> int:
        """Count the number of users records present in the database

        Parameters
        ----------
        select
            Select the users fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            users filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.usersCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await users.prisma().count()

        # results: prisma.types.usersCountAggregateOutput
        results = await users.prisma().count(
            select={
                '_all': True,
                'username': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.usersCountAggregateInput,
        take: int | None = None,
        skip: int | None = None,
        where: types.usersWhereInput | None = None,
        cursor: types.usersWhereUniqueInput | None = None,
    ) -> types.usersCountAggregateOutput:
        ...

    async def count(
        self,
        select: types.usersCountAggregateInput | None = None,
        take: int | None = None,
        skip: int | None = None,
        where: types.usersWhereInput | None = None,
        cursor: types.usersWhereUniqueInput | None = None,
    ) -> int | types.usersCountAggregateOutput:
        """Count the number of users records present in the database

        Parameters
        ----------
        select
            Select the users fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            users filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.usersCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await users.prisma().count()

        # results: prisma.types.usersCountAggregateOutput
        results = await users.prisma().count(
            select={
                '_all': True,
                'email': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.usersCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: types.usersWhereInput | None = None
    ) -> int:
        """Delete multiple users records.

        Parameters
        ----------
        where
            Optional users filter to find the records to be deleted

        Returns
        -------
        int
            The total number of users records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all users records
        total = await users.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: list[types.usersScalarFieldKeys],
        *,
        where: types.usersWhereInput | None = None,
        take: int | None = None,
        skip: int | None = None,
        avg: types.usersAvgAggregateInput | None = None,
        sum: types.usersSumAggregateInput | None = None,
        min: types.usersMinAggregateInput | None = None,
        max: types.usersMaxAggregateInput | None = None,
        having: types.usersScalarWhereWithAggregatesInput | None = None,
        count: bool | types.usersCountAggregateInput | None = None,
        order: Mapping[types.usersScalarFieldKeys, types.SortOrder] | list[Mapping[types.usersScalarFieldKeys, types.SortOrder]] | None = None,
    ) -> list[types.usersGroupByOutput]:
        """Group users records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar users fields to group records by
        where
            users filter to select records
        take
            Limit the maximum number of users records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.usersGroupByOutput]
            A list of dictionaries representing the users record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group users records by id values
        # and count how many records are in each group
        results = await users.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: list[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


